此题目一开始明确是进制转换,最基本的是暴力搜索尝试所有进制,直至成功.
函数isPalindrome(n, k)就是用来判断k进制下n是否为回文数.
但可以料想时间复杂度有O(n),针对题目中n的范围,这种效率并不理想.
观察可以发现,O(n)的复杂度中从n^0.5~n-1进制下,n的位数是2,可表示为xx,
此时可以列写出简单方程x*k+x=n,只需枚举x从n^0.5~1,即可求得k,
再针对边界情况设置特判处理,由此该题复杂度降至O(n^0.5)*T,已满足题目需求.
