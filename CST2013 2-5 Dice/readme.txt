题目中某一格子上的方块转向周围,跟它此时的状态有关,
所以可以把方块的状态和位置组合起来,视作一个点,那每个点至多四条边,
指向相邻位置的不同状态的方块,其中边的权值是转完后方块的底面数字,
从初始时确定的节点出发,找出到其余所有点的最短路,
利用第四题时使用的二叉堆优化的Dijkstra算法,
可以完成任务,且复杂度是O((n + m) * log(n)),
由于m<=4*n,所以复杂度最终是O(n * log(n)),其中n是格子数乘以常数(方块的状态数).
符合题目要求.
