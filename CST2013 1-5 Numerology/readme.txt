该题目经仔细思考后,可以发现,从左到右可以算出当前已有的大小,
而从右到左可以得出当前需要的大小,只要两者匹配的上,就是所求答案,
所以可以把表达式一拆为二,左右分别计算,再排序后匹配,
复杂度是O(n*3^(n/2))可以接受,
接着为了不漏去中间连接的情况,令中间不是运算符,向右枚举中间数的末尾位置,
每次重算左右两式并排序匹配,复杂度依然是O(n*3^(n/2)),
故总的复杂度是O(n^2*3^(n/2)),满足了题目的要求.
